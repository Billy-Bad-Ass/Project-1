{
  "name": "bba-network-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Stripe webhook, one-click unsubscribe, and the shared suppression store.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsx --test \"src/**/*.test.ts\"",
    "check": "npm run typecheck && npm test",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20250109.0",
    "@types/node": "22.10.7",
    "tsx": "4.23.12",
    "typescript": "5.7.3",
    "wrangler": "3.99.0"
  }
}
